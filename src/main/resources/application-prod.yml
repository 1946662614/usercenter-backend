spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "123456"
    url: jdbc:mysql://localhost:3306/yupi
#    session失效时间
  session:
    timeout: 86400

